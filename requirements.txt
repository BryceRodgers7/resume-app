# Core
streamlit==1.29.0

# AI/ML Libraries
# Note: torch and torchvision are installed separately in Dockerfile (CPU-only version)
# torch>=2.0.0  # Installed via Dockerfile
# torchvision>=0.15.0  # Installed via Dockerfile
transformers>=4.35.0
openai==1.7.0
# anthropic>=0.7.0  # Not currently used

# Image Processing
pillow>=10.0.0
opencv-python-headless>=4.8.0  # Headless version (no GUI, smaller size)

# HTTP & API
requests>=2.31.0
httpx>=0.25.0
# ddgs==9.10.0  # Commented out - not needed for production

# Data Science
numpy>=1.24.0
pandas>=2.0.0

# Optional: Stability AI
# stability-sdk>=0.8.0  # Not needed - using requests directly

# Optional: For advanced features
python-dotenv>=1.0.0

# Qdrant
qdrant-client==1.7.0

# Database
psycopg2-binary==2.9.9

# Numpy
numpy<2

